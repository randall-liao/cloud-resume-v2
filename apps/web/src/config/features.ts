/**
 * Application feature flags.
 * Toggle these values to enable or disable features across the application.
 */
export const FEATURES = {
  /**
   * Set this to true once the backend visitor counter API is implemented and ready.
   * When false, the visitor counter in the footer is hidden.
   */
  enableVisitorCounter: false,
};
