import React from 'react';
import ReactDOM from 'react-dom/client';
import SpyfallIntro from './SpyfallIntro';
import './spyfall.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SpyfallIntro />
  </React.StrictMode>,
);
