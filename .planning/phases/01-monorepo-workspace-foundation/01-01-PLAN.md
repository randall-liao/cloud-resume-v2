---
phase: 01-monorepo-workspace-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - apps/web/package.json
  - apps/web/index.html
  - apps/web/src
  - apps/web/public
  - apps/web/tsconfig.json
  - apps/web/tsconfig.node.json
  - apps/web/vite.config.ts
  - apps/web/tailwind.config.js
  - apps/web/postcss.config.js
autonomous: true
requirements:
  - WS-01
  - WS-02
user_setup: []
must_haves:
  truths:
    - The repository can be installed and run from the monorepo root with workspace-aware commands.
    - The existing resume SPA lives under apps/web without losing its static build behavior.
    - The moved web app still builds successfully from its workspace.
  artifacts:
    - pnpm-workspace.yaml
    - package.json
    - apps/web/package.json
    - apps/web/src/App.tsx
    - apps/web/vite.config.ts
  key_links:
    - Root scripts delegate to the web workspace instead of assuming a single-package root.
    - The web workspace owns its source, public assets, and build configuration after the move.
    - The workspace build still emits a static frontend artifact from the web app package.
---

<objective>
Establish the monorepo root and relocate the existing frontend into `apps/web`.

Purpose: make the repository structurally ready for multiple domains without rewriting the current app.
Output: a working workspace root plus a migrated `apps/web` package that still boots and builds.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-workspace-foundation/01-RESEARCH.md
@package.json
@src/App.tsx
@src/main.tsx
@vite.config.ts
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the workspace root around pnpm</name>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml</files>
  <action>Convert the current root manifest into a workspace root that uses pnpm. Add a root-level packageManager declaration, keep the repo private, and define root scripts that target the web workspace for dev, build, lint, and validate. Do not introduce Turborepo or Nx in this phase; keep the root task surface simple and explicit.</action>
  <verify>pnpm install && pnpm -r list --depth -1</verify>
  <done>The repo installs from the root with pnpm and workspace discovery is working.</done>
</task>

<task type="auto">
  <name>Task 2: Move the current SPA into apps/web</name>
  <files>apps/web/package.json, apps/web/index.html, apps/web/src, apps/web/public, apps/web/tsconfig.json, apps/web/tsconfig.node.json, apps/web/vite.config.ts, apps/web/tailwind.config.js, apps/web/postcss.config.js</files>
  <action>Relocate the current frontend app files into `apps/web` while preserving the current runtime behavior. Give the web app its own package manifest and keep its build chain local to the app package. Avoid mixing in shared-package extraction; the goal here is relocation with parity, not package decomposition.</action>
  <verify>pnpm --filter web build</verify>
  <done>The web app source, assets, and config live under `apps/web`, and the workspace build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Repair app-local path assumptions after the move</name>
  <files>apps/web/package.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/index.html</files>
  <action>Update app-local path assumptions so the moved package works from its new location. Resolve any root-relative config or asset references that break when the app is no longer at repository root. Keep the output model as a static frontend artifact owned by the web app package.</action>
  <verify>pnpm --filter web dev --host 127.0.0.1 --port 4173</verify>
  <done>The app starts from the workspace package without path or config errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` succeeds from repo root
- [ ] `pnpm --filter web build` succeeds
- [ ] The moved app can start from `apps/web` without broken config or asset paths
</verification>

<success_criteria>

- All tasks completed
- Root workspace install and script delegation are functional
- The frontend now lives in `apps/web`
- Static build behavior is preserved after relocation
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-workspace-foundation/01-01-SUMMARY.md`
</output>
