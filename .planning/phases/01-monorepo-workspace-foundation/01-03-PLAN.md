---
phase: 01-monorepo-workspace-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - apps/README.md
  - packages/README.md
  - packages/contracts/README.md
  - services/README.md
  - infra/README.md
  - docs/README.md
  - docs/monorepo.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - AGT-03
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - Future platform domains have explicit, documented homes in the repository.
    - Agents can determine ownership and dependency direction for app, package, and future platform work from repository-local docs.
    - Git subtree boundaries are documented at the domain level rather than improvised later.
  artifacts:
    - docs/monorepo.md
    - apps/README.md
    - packages/README.md
    - packages/contracts/README.md
    - services/README.md
    - infra/README.md
  key_links:
    - Root docs link to the detailed monorepo boundary document.
    - Placeholder domain directories explain what belongs there and what does not.
    - The git subtree strategy maps to stable top-level domain boundaries.
---

<objective>
Document domain ownership and future platform boundaries for the monorepo.

Purpose: make the repository legible to multiple agents before backend and infrastructure work begins.
Output: explicit placeholder domain docs plus a monorepo boundary document that records ownership and subtree rules.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-workspace-foundation/01-RESEARCH.md
@docs/README.md
@docs/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder domain directories with ownership docs</name>
  <files>apps/README.md, packages/README.md, packages/contracts/README.md, services/README.md, infra/README.md</files>
  <action>Create stable top-level domain directories for the monorepo and give each a README that explains purpose, allowed contents, and ownership expectations. Reserve `packages/contracts` as the future home for shared backend or database-facing contracts without implementing those contracts now. Prefer committed documentation over empty folders so agents see the future boundaries directly in-repo.</action>
  <verify>test -f apps/README.md && test -f packages/README.md && test -f packages/contracts/README.md && test -f services/README.md && test -f infra/README.md</verify>
  <done>Each top-level domain exists in Git with a short ownership document, including a reserved contracts boundary under `packages/`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the detailed monorepo boundary and subtree guide</name>
  <files>docs/monorepo.md, docs/README.md</files>
  <action>Write a monorepo architecture document that explains the purpose of each domain, dependency direction rules, and how git subtree should map to future domain extraction or sync. Keep subtree guidance at the domain level (`apps/web`, shared packages, future services, future infra) and explicitly discourage tiny-package subtree boundaries.</action>
  <verify>pnpm docs:validate</verify>
  <done>The repo has a detailed monorepo boundary document linked from the docs index.</done>
</task>

<task type="auto">
  <name>Task 3: Align planning memory with the new repository contract</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Update planning artifacts so they reflect the concrete domain boundaries, ownership model, and future platform placeholders established by this phase. Do not expand scope into backend implementation; keep the planning memory aligned with the structural outcome only.</action>
  <verify>node \"$HOME/.codex/get-shit-done/bin/gsd-tools.cjs\" roadmap analyze</verify>
  <done>The planning artifacts describe the same monorepo boundaries that now exist in the repository.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Placeholder domain directories exist with README files
- [ ] `pnpm docs:validate` succeeds
- [ ] Planning artifacts reflect the final domain boundary model
</verification>

<success_criteria>

- All tasks completed
- Future platform boundaries are explicit and repository-local
- Agents can determine ownership and subtree rules from the docs
- Phase planning artifacts match the implemented monorepo structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-workspace-foundation/01-03-SUMMARY.md`
</output>
