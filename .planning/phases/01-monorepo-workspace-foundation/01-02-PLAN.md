---
phase: 01-monorepo-workspace-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package.json
  - AGENTS.md
  - README.md
  - docs/README.md
  - docs/architecture.md
  - docs/quality_standards.md
  - scripts/validate-docs.mjs
  - scripts/validate-dist.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - WS-01
  - AGT-03
user_setup: []
must_haves:
  truths:
    - Root docs describe the monorepo layout instead of the old single-package shape.
    - Root validation commands work from the monorepo root after the web app move.
    - CI and repository scripts no longer assume `src/` and `dist/` live at repo root.
  artifacts:
    - AGENTS.md
    - README.md
    - docs/architecture.md
    - scripts/validate-docs.mjs
    - scripts/validate-dist.sh
    - .github/workflows/ci.yml
  key_links:
    - Root docs point agents to the web workspace and monorepo source-of-truth files.
    - Validation scripts target workspace paths correctly.
    - CI uses the same workspace-aware commands that local agents and humans run.
---

<objective>
Rewire root docs, scripts, and CI for the new workspace topology.

Purpose: keep the agent map and validation surface truthful after the web app move.
Output: workspace-aware root docs, validation scripts, and CI wiring.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-monorepo-workspace-foundation/01-RESEARCH.md
@AGENTS.md
@README.md
@docs/architecture.md
@scripts/validate-docs.mjs
@scripts/validate-dist.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore root validation and developer ergonomics</name>
  <files>package.json, scripts/validate-docs.mjs, scripts/validate-dist.sh</files>
  <action>Update the root command surface so `dev`, `build`, `lint`, and `validate` still work from the monorepo root after the move. Make validation scripts path-aware for the web workspace and avoid hard-coding stale single-package assumptions.</action>
  <verify>pnpm validate</verify>
  <done>The repo has a single root validation entrypoint that succeeds against the workspace layout.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the root agent and human docs</name>
  <files>AGENTS.md, README.md, docs/README.md, docs/architecture.md, docs/quality_standards.md</files>
  <action>Rewrite the root docs so they describe the monorepo layout, the location of the web app, and the new repo navigation rules. Keep `docs/` as the canonical knowledge base and avoid duplicating deep implementation detail into `AGENTS.md`.</action>
  <verify>pnpm docs:validate</verify>
  <done>Root docs and the agent map point to the correct workspace locations with no stale single-package paths.</done>
</task>

<task type="auto">
  <name>Task 3: Make CI match the monorepo root commands</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>Update CI so it runs the same workspace-aware commands used locally. Keep the pipeline simple and deterministic: install dependencies from the root, run root validation, and ensure the web app build path is covered through the root workflow.</action>
  <verify>pnpm validate</verify>
  <done>CI configuration references root workspace commands instead of obsolete root-app file locations.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm docs:validate` succeeds
- [ ] `pnpm validate` succeeds from repo root
- [ ] CI config references workspace-aware commands and paths
</verification>

<success_criteria>

- All tasks completed
- Root docs truthfully describe the monorepo
- Root validation works after the app move
- CI and local commands are aligned around the workspace root
</success_criteria>

<output>
After completion, create `.planning/phases/01-monorepo-workspace-foundation/01-02-SUMMARY.md`
</output>
