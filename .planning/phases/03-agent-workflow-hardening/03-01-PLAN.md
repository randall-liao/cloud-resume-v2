---
phase: 03-agent-workflow-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .github/workflows/ci.yml
  - docs/principles.md
  - docs/quality.md
  - docs/quality_standards.md
autonomous: true
requirements:
  - VAL-01
user_setup: []
must_haves:
  truths:
    - Root validation and CI understand that the repo contains a web app plus shared packages.
    - Shared packages are validated through the root command surface instead of only through app integration.
    - Documentation for enforced validation gates matches the real command surface.
  artifacts:
    - package.json
    - .github/workflows/ci.yml
    - docs/principles.md
  key_links:
    - Root scripts expose package-aware build commands.
    - CI reuses the root validation model instead of app-only one-off checks.
    - Enforcement docs are updated in the same change as validation rules.
---

<objective>
Make root validation and CI workspace-aware.

Purpose: stop treating the monorepo like a single-app repo and encode the real package boundaries into the root command surface and CI.
Output: root scripts and CI that validate the shared packages plus the web app, with docs updated to match.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agent-workflow-hardening/03-CONTEXT.md
@.planning/phases/03-agent-workflow-hardening/03-RESEARCH.md
@package.json
@.github/workflows/ci.yml
@docs/principles.md
@docs/quality.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the root command surface for workspace-aware validation</name>
  <files>package.json</files>
  <action>Add explicit root scripts for package-aware builds and web-app builds so agents can validate the repo through readable top-level commands. Keep the command surface explicit rather than relying on opaque workspace fan-out.</action>
  <verify>npm run build:packages && npm run build:web</verify>
  <done>The root manifest exposes workspace-aware build commands that cover the shared packages and the active app.</done>
</task>

<task type="auto">
  <name>Task 2: Align CI with the workspace-aware root validation model</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <action>Update CI so it validates the actual monorepo structure instead of focusing only on `apps/web`. Reuse the root command surface where possible so local and CI behavior stay aligned.</action>
  <verify>npm run docs:validate && npm run lint && npm run build:packages && npm run build:web</verify>
  <done>CI and local workflows validate the shared packages and the active app through the same root-level intent.</done>
</task>

<task type="auto">
  <name>Task 3: Update validation docs to match the new gate surface</name>
  <files>docs/principles.md, docs/quality.md, docs/quality_standards.md</files>
  <action>Refresh the enforcement and quality docs so they describe the real validation surface after the workspace-aware command changes. Treat this as part of the implementation, not cleanup.</action>
  <verify>npm run docs:validate</verify>
  <done>The repo’s documented validation gates match the actual root commands and CI behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build:packages` succeeds
- [ ] `npm run build:web` succeeds
- [ ] CI configuration no longer assumes the web app is the only meaningful validation target
</verification>

<success_criteria>

- All tasks completed
- Root validation and CI are workspace-aware
- Shared packages are part of the validation surface
- Validation docs reflect the real enforcement model
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-workflow-hardening/03-01-SUMMARY.md`
</output>
