---
phase: 03-agent-workflow-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - AGENTS.md
  - docs/README.md
  - docs/monorepo.md
  - docs/git_subtree.md
  - packages/README.md
  - services/README.md
  - infra/README.md
  - scripts/validate-docs.mjs
autonomous: true
requirements:
  - AGT-01
  - AGT-02
user_setup: []
must_haves:
  truths:
    - Agents can determine where work belongs across app, package, service, and infra domains from repo-local docs.
    - The repo documents an operational git subtree workflow, not just high-level subtree candidates.
    - The docs validator treats the subtree workflow doc as part of the canonical knowledge base if it becomes required.
  artifacts:
    - docs/git_subtree.md
    - docs/monorepo.md
    - AGENTS.md
    - scripts/validate-docs.mjs
  key_links:
    - Root agent docs point to the subtree workflow and ownership rules.
    - Domain READMEs and monorepo docs agree on ownership boundaries.
    - The docs validator enforces the new canonical doc surface.
---

<objective>
Document git subtree workflow and multi-agent ownership rules.

Purpose: turn the repo’s existing high-level boundaries into an operational collaboration guide that future agents can follow without extra human explanation.
Output: canonical docs for subtree workflow and domain ownership, wired into the root docs map and validation surface.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agent-workflow-hardening/03-CONTEXT.md
@.planning/phases/03-agent-workflow-hardening/03-RESEARCH.md
@AGENTS.md
@docs/README.md
@docs/monorepo.md
@packages/README.md
@services/README.md
@infra/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a canonical operational subtree workflow document</name>
  <files>docs/git_subtree.md, docs/monorepo.md</files>
  <action>Create a dedicated subtree workflow document that explains when subtree is appropriate, which directories are good candidates, and how agents should perform add/pull/push flows. Keep `docs/monorepo.md` as the short boundary map and use the new doc for procedural detail.</action>
  <verify>rg -n "git subtree|subtree add|subtree pull|subtree push" docs/monorepo.md docs/git_subtree.md</verify>
  <done>The repo has a canonical subtree operations guide that matches the monorepo boundary map.</done>
</task>

<task type="auto">
  <name>Task 2: Align root and domain docs with multi-agent ownership rules</name>
  <files>AGENTS.md, docs/README.md, packages/README.md, services/README.md, infra/README.md</files>
  <action>Refresh the root and domain docs so agents can tell where frontend, shared package, service, and infrastructure work belongs, and when a task should stay in one domain versus crossing boundaries.</action>
  <verify>npm run docs:validate</verify>
  <done>The root docs and domain docs provide a consistent ownership model for future agent work.</done>
</task>

<task type="auto">
  <name>Task 3: Make the subtree workflow part of the validated knowledge base</name>
  <files>scripts/validate-docs.mjs, docs/README.md, AGENTS.md</files>
  <action>If the subtree workflow doc becomes canonical, add it to the validated docs surface and ensure the root docs map points agents to it explicitly.</action>
  <verify>npm run docs:validate</verify>
  <done>The subtree workflow is discoverable from the root map and mechanically protected from drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/git_subtree.md` exists and contains operational subtree command guidance
- [ ] `npm run docs:validate` succeeds
- [ ] Root and domain docs agree on ownership and subtree boundaries
</verification>

<success_criteria>

- All tasks completed
- Subtree workflow is documented operationally
- Agents can determine ownership from repo-local docs
- The canonical docs surface includes the new workflow guidance
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-workflow-hardening/03-03-SUMMARY.md`
</output>
