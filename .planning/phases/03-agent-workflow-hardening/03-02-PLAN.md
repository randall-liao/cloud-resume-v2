---
phase: 03-agent-workflow-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - package-lock.json
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/src/test/setup.ts
  - apps/web/src/test/App.smoke.test.tsx
  - .github/workflows/ci.yml
  - docs/quality.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements:
  - VAL-02
user_setup: []
must_haves:
  truths:
    - The web app has an automated smoke path that runs from the monorepo root.
    - The smoke suite protects at least app boot/content rendering and theme behavior.
    - Root validation and CI run the smoke suite as part of normal verification.
  artifacts:
    - apps/web/package.json
    - apps/web/src/test/setup.ts
    - apps/web/src/test/App.smoke.test.tsx
    - package.json
  key_links:
    - Root test commands fan into the web workspace.
    - The smoke tests exercise the shared package seams through the running app.
    - Documentation and testing maps are updated together with the harness.
---

<objective>
Add smoke tests for the migrated web app.

Purpose: protect user-visible app boot and critical interaction paths now that the frontend depends on shared packages and multiple agents can refactor it in parallel.
Output: a lightweight smoke test harness plus at least one root-runnable smoke path in CI.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-agent-workflow-hardening/03-CONTEXT.md
@.planning/phases/03-agent-workflow-hardening/03-RESEARCH.md
@apps/web/src/App.tsx
@apps/web/src/components/Header.tsx
@apps/web/src/components/Footer.tsx
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure a lightweight smoke-test harness for `apps/web`</name>
  <files>package.json, package-lock.json, apps/web/package.json, apps/web/vite.config.ts, apps/web/src/test/setup.ts</files>
  <action>Add a Vite-compatible test harness for the web app using Vitest and React Testing Library. Expose it through readable root and workspace scripts so agents can run the suite from the repo root.</action>
  <verify>npm run test --workspace @cloud-resume-v2/web -- --run</verify>
  <done>The web workspace can run tests in non-watch mode through a stable command surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke coverage for app boot and theme behavior</name>
  <files>apps/web/src/test/App.smoke.test.tsx, apps/web/src/App.tsx, apps/web/src/components/Header.tsx</files>
  <action>Create a smoke test that proves the app renders key resume content from the shared contracts package and that theme interaction still updates the expected browser-visible state. Keep the suite small and stable.</action>
  <verify>npm run test --workspace @cloud-resume-v2/web -- --run</verify>
  <done>The app has at least one automated smoke path protecting critical boot behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire smoke tests into root validation and CI, then update testing docs</name>
  <files>package.json, .github/workflows/ci.yml, docs/quality.md, .planning/codebase/TESTING.md</files>
  <action>Extend the root validation flow and CI so the smoke tests run during normal verification. Update testing docs and quality tracking to reflect the new automated coverage honestly.</action>
  <verify>npm run validate</verify>
  <done>Smoke coverage is part of the normal root validation path and documented as such.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test --workspace @cloud-resume-v2/web -- --run` succeeds
- [ ] `npm run validate` succeeds
- [ ] CI includes the smoke suite in normal verification
</verification>

<success_criteria>

- All tasks completed
- The repo has a root-runnable smoke suite
- Shared package integration is protected through app-level smoke coverage
- Testing docs reflect the new coverage truthfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-workflow-hardening/03-02-SUMMARY.md`
</output>
