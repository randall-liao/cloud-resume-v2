---
phase: 02-shared-frontend-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/frontend-core/package.json
  - packages/frontend-core/tsconfig.json
  - packages/frontend-core/src/index.ts
  - packages/frontend-core/src/themeManager.ts
  - packages/frontend-core/README.md
autonomous: true
requirements:
  - FE-02
user_setup: []
must_haves:
  truths:
    - Theme persistence is owned by a package-level API instead of app-local utility code.
    - Browser-specific frontend helpers are isolated away from shared contracts.
    - The new package remains intentionally narrow and does not become a generic dumping ground.
  artifacts:
    - packages/frontend-core/package.json
    - packages/frontend-core/tsconfig.json
    - packages/frontend-core/src/index.ts
    - packages/frontend-core/src/themeManager.ts
  key_links:
    - packages/frontend-core is the shared runtime helper seam for current and future frontend apps.
    - Theme persistence logic is exposed through an explicit package entrypoint.
    - Browser APIs stay out of `packages/contracts`.
---

<objective>
Extract the shared theme and frontend utility package in `packages/frontend-core`.

Purpose: give the monorepo a dedicated package for browser-facing shared frontend helpers without polluting the contracts package or leaving utility ownership inside the web app.
Output: a real `@cloud-resume-v2/frontend-core` workspace package exporting the theme persistence API.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-frontend-contracts/02-CONTEXT.md
@.planning/phases/02-shared-frontend-contracts/02-RESEARCH.md
@apps/web/src/utils/themeManager.ts
@docs/monorepo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the `frontend-core` workspace package</name>
  <files>packages/frontend-core/package.json, packages/frontend-core/tsconfig.json, packages/frontend-core/src/index.ts, packages/frontend-core/README.md</files>
  <action>Create a workspace package for reusable frontend runtime helpers. Keep its role explicit: browser-facing frontend support code that may be shared across apps, but not app-specific presentation code and not cross-domain contracts.</action>
  <verify>npm run build --workspace @cloud-resume-v2/frontend-core</verify>
  <done>The `frontend-core` package exists as a buildable workspace package with an explicit public entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Move theme persistence into the package and define the public API</name>
  <files>packages/frontend-core/src/themeManager.ts, packages/frontend-core/src/index.ts</files>
  <action>Move the current theme persistence helper into the package and expose the right API for app consumption. Preserve browser safety, persistence semantics, and dark-mode initialization behavior while removing the need for `apps/web` to own this helper locally.</action>
  <verify>npm run build --workspace @cloud-resume-v2/frontend-core</verify>
  <done>The package exports the theme helper API needed by the app without changing user-visible theme behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the package intentionally narrow</name>
  <files>packages/frontend-core/README.md, packages/frontend-core/src/index.ts</files>
  <action>Review exports and docs so the package stays focused on shared frontend runtime helpers. Avoid creating a general `utils` dumping ground; only export what Phase 2 actually needs to remove app-local theme ownership.</action>
  <verify>npm run build --workspace @cloud-resume-v2/frontend-core</verify>
  <done>`frontend-core` has a clear ownership rule and a small discoverable API surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --workspace @cloud-resume-v2/frontend-core` succeeds
- [ ] Theme persistence behavior is still represented by a package export
- [ ] Browser-only helpers remain out of `packages/contracts`
</verification>

<success_criteria>

- All tasks completed
- `packages/frontend-core` exists as a real workspace package
- Theme persistence is exposed through a package API
- The package remains narrowly scoped to shared frontend runtime concerns
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-frontend-contracts/02-02-SUMMARY.md`
</output>
