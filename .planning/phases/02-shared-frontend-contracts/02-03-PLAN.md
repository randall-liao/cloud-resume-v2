---
phase: 02-shared-frontend-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - apps/web/package.json
  - apps/web/src/App.tsx
  - apps/web/src/components/Header.tsx
  - apps/web/src/components/Hero.tsx
  - apps/web/src/components/OriginStory.tsx
  - apps/web/src/components/CommitHistory.tsx
  - apps/web/src/components/Education.tsx
  - apps/web/src/components/Certifications.tsx
  - apps/web/src/components/Interests.tsx
  - apps/web/src/components/Footer.tsx
  - apps/web/src/data/resume.json
  - apps/web/src/utils/themeManager.ts
  - README.md
  - docs/architecture.md
  - docs/monorepo.md
  - docs/quality.md
autonomous: true
requirements:
  - WS-03
  - FE-01
  - FE-02
user_setup: []
must_haves:
  truths:
    - The web app consumes package APIs instead of direct app-local data and utility imports.
    - Root and architecture docs reflect the extracted package boundaries truthfully.
    - The full repo validation entrypoint still passes after the migration.
  artifacts:
    - apps/web/package.json
    - apps/web/src/App.tsx
    - apps/web/src/components
    - README.md
    - docs/architecture.md
  key_links:
    - `apps/web` depends on packages through workspace package names rather than leaf file paths.
    - `packages/contracts` owns resume content and `packages/frontend-core` owns theme persistence.
    - Agents can discover the new boundaries from both imports and docs.
---

<objective>
Migrate the web app to the new package APIs and remove direct leaf imports.

Purpose: finish the package extraction by making `apps/web` depend on stable workspace boundaries instead of raw internal files, then update docs so future agents see the real architecture.
Output: a working web app that imports shared content and theme helpers from workspace packages, with stale local sources removed or retired.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-frontend-contracts/02-CONTEXT.md
@.planning/phases/02-shared-frontend-contracts/02-RESEARCH.md
@.planning/phases/02-shared-frontend-contracts/02-VALIDATION.md
@apps/web/src/App.tsx
@apps/web/src/components/Footer.tsx
@docs/architecture.md
@docs/monorepo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire `apps/web` to workspace package dependencies</name>
  <files>apps/web/package.json, apps/web/src/App.tsx, apps/web/src/components/Header.tsx, apps/web/src/components/Hero.tsx, apps/web/src/components/OriginStory.tsx, apps/web/src/components/CommitHistory.tsx, apps/web/src/components/Education.tsx, apps/web/src/components/Certifications.tsx, apps/web/src/components/Interests.tsx, apps/web/src/components/Footer.tsx</files>
  <action>Add the new workspace dependencies to the web app and replace direct imports from `./data/resume.json`, `../data/resume.json`, and local theme utility files with package imports from `@cloud-resume-v2/contracts` and `@cloud-resume-v2/frontend-core`.</action>
  <verify>npm run build</verify>
  <done>The app compiles while consuming the package APIs through workspace dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale leaf sources and cleanup local ownership confusion</name>
  <files>apps/web/src/data/resume.json, apps/web/src/utils/themeManager.ts, apps/web/src/App.tsx</files>
  <action>Delete or retire app-local sources that are no longer the ownership boundary after package extraction. Ensure there are no remaining direct imports to those old paths in `apps/web/src`.</action>
  <verify>! rg -n "data/resume.json|utils/themeManager" apps/web/src</verify>
  <done>No app code imports the old local data or theme utility paths.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs to match the extracted package architecture</name>
  <files>README.md, docs/architecture.md, docs/monorepo.md, docs/quality.md</files>
  <action>Update root and architecture docs so they describe the real package boundaries, public ownership rules, and current import direction. Keep the docs concise and accurate to the code so agents do not follow stale architecture guidance.</action>
  <verify>npm run validate</verify>
  <done>The code and docs agree on the Phase 2 architecture, and root validation passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run validate` succeeds
- [ ] `apps/web` imports workspace packages instead of local leaf data or theme helpers
- [ ] Docs reflect `packages/contracts` and `packages/frontend-core` as real boundaries
</verification>

<success_criteria>

- All tasks completed
- The web app depends on package APIs under `packages/`
- Direct local imports of `resume.json` and `themeManager` are gone from `apps/web`
- Docs and validation reflect the new architecture
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-frontend-contracts/02-03-SUMMARY.md`
</output>
