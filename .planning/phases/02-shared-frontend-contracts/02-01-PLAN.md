---
phase: 02-shared-frontend-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/index.ts
  - packages/contracts/src/resume.ts
  - packages/contracts/src/resume.json
  - packages/contracts/README.md
autonomous: true
requirements:
  - FE-01
user_setup: []
must_haves:
  truths:
    - Resume content is owned by a workspace package instead of app-local source files.
    - The contracts package exports a typed public API for the resume document.
    - The package remains browser-agnostic so future backend code can consume it.
  artifacts:
    - packages/contracts/package.json
    - packages/contracts/tsconfig.json
    - packages/contracts/src/index.ts
    - packages/contracts/src/resume.ts
  key_links:
    - packages/contracts becomes the source of truth for shared resume content.
    - The exported contracts package API is small enough for future agents to discover quickly.
    - Raw data is parsed once at the package boundary before the app consumes it.
---

<objective>
Extract a typed resume content package in `packages/contracts`.

Purpose: remove app-local ownership of the resume document and establish a shared contract boundary that future frontend and backend code can reuse.
Output: a real `@cloud-resume-v2/contracts` workspace package that exports typed resume data through a stable entrypoint.
</objective>

<execution_context>
@/home/devuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/devuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-frontend-contracts/02-CONTEXT.md
@.planning/phases/02-shared-frontend-contracts/02-RESEARCH.md
@apps/web/src/data/resume.json
@packages/contracts/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert `packages/contracts` into a real workspace package</name>
  <files>packages/contracts/package.json, packages/contracts/tsconfig.json, packages/contracts/src/index.ts, packages/contracts/README.md</files>
  <action>Create a proper workspace package for shared contracts with a package name, package scripts, and a single public entrypoint. Keep the package browser-agnostic and document its role as shared frontend and future backend contract space.</action>
  <verify>npm run build --workspace @cloud-resume-v2/contracts</verify>
  <done>The contracts package exists as a real workspace package and can be built or type-checked in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Move the resume source into the contracts package and export typed data</name>
  <files>packages/contracts/src/resume.ts, packages/contracts/src/resume.json, packages/contracts/src/index.ts</files>
  <action>Move the canonical resume document into `packages/contracts` and expose a typed API such as `resumeData` plus `ResumeDocument`. Parse or assert the shape at the package boundary so the rest of the app does not rely on guessed data structure.</action>
  <verify>npm run build --workspace @cloud-resume-v2/contracts</verify>
  <done>The package exports a typed resume contract from its root entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the contracts package future-safe for multi-domain reuse</name>
  <files>packages/contracts/README.md, packages/contracts/src/index.ts, packages/contracts/src/resume.ts</files>
  <action>Review the public API and package docs so the boundary is explicit for future consumers. Keep browser-only helpers and app-specific presentation logic out of this package, and avoid over-splitting section-specific exports before the top-level document contract is stable.</action>
  <verify>npm run build --workspace @cloud-resume-v2/contracts</verify>
  <done>The contracts package is clearly shaped for reuse by `apps/web` now and future platform domains later.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build --workspace @cloud-resume-v2/contracts` succeeds
- [ ] The package exports a typed root API for the resume document
- [ ] No browser-only code lives in `packages/contracts`
</verification>

<success_criteria>

- All tasks completed
- `packages/contracts` is a real workspace package
- Resume content ownership has moved out of `apps/web`
- The package exposes a stable, typed contract boundary
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-frontend-contracts/02-01-SUMMARY.md`
</output>
